insert into flights (
select
to_utc_timestamp(substr(DepartureTS,1,19),timezoneId),
to_utc_timestamp(substr(CRSDepartureTS,1,19),timezoneId),
to_utc_timestamp(substr(ArrivalTS,1,19),timezoneId_dest),
to_utc_timestamp(substr(CRSArrivalTS,1,19),timezoneId_dest),
UniqueCarrier,
Description,
FlightNum,
TailNum,
Origin,
airport,
city,
state,
lat,
lon,
rawOffset,
timezoneId,
Dest,
airport_dest,
city_dest,
state_dest,
lat_dest,
lon_dest,
rawOffset_dest,
timezoneId_dest,
Cancelled,
CancellationCode,
Diverted,
TaxiIn,
TaxiOut,
Distance,
ActualElapsedTime,
CRSElapsedTime,
AirTime,
ArrDelay,
DepDelay,
CarrierDelay,
WeatherDelay,
NASDelay,
SecurityDelay,
LateAircraftDelay
from stage_flights
)